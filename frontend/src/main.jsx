import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => (
  <div>
    <h1>CloudHub Metadata Comparator</h1>
    <p>This UI will eventually show diffed metadata from multiple environments.</p>
  </div>
);

ReactDOM.createRoot(document.getElementById('root')).render(<App />);
